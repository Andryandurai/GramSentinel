# GramSentinel — backend requirements (Django + DRF stack, per finalized spec)
Django==5.0.6
djangorestframework==3.15.2
djangorestframework-simplejwt==5.3.1
django-cors-headers==4.4.0
channels==4.1.0
daphne==4.1.2
python-dotenv==1.0.1

# Analytics / lightweight statistics (no model training)
pandas==2.2.2
numpy==1.26.4
scikit-learn==1.5.0

# LLM (optional — system must work without it)
requests==2.32.3

# Production serving
gunicorn==22.0.0
whitenoise==6.7.0

# Testing
pytest==8.2.2
pytest-django==4.8.0
